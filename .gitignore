
# Look-development and probe renders: hundreds of megabytes of intermediate
# frames that only mattered while the burger was being built. The finished
# sequence under site/frames/ is NOT ignored — ignoring it silently turned the
# render job's commit into a no-op.
research/run4-blender-handoff/out/look/
research/run4-blender-handoff/out/probe*/
research/run4-blender-handoff/out/seq/
site/dist/frames/
__pycache__/

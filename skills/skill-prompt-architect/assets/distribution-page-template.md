# Distribution Page Template (modeled on the smooth-brain page)

Use for any skill shared with non-technical users (Notion / web page / README). Replace ALL-CAPS slots. Keep the voice: plain, funny-adjacent, zero jargon, second person.

---

# SKILL-NAME — OUTCOME IN PLAIN WORDS

One skill file that turns Claude from "OLD BEHAVIOR" into "NEW BEHAVIOR". Drop it in once, it registers for good — no prompts to re-paste, no code, no Claude Code. Just normal claude.ai, finally DESIRED-ADJECTIVE.

### 📎 Download
> SKILL-NAME.skill — SIZE KiB

### What it actually does
PAIN PARAGRAPH — describe the default Claude behavior in the user's own frustration language. ("You ask a yes-or-no question and it hands you a three-paragraph essay… Nobody asked for the TED Talk.")

Same question, before and after:

> **You:** FLAGSHIP PROMPT
> **Stock Claude:** REALISTIC HEDGING ANSWER, 2–3 lines
> **With `SKILL-NAME` loaded:** THE EXACT TARGET RESPONSE

That's the whole response. That's it.

And it works on everything:

| You ask | Claude does |
| --- | --- |
| "EXAMPLE 1" | BEHAVIOR 1 |
| "EXAMPLE 2" | BEHAVIOR 2 |
| "EXAMPLE 3" | BEHAVIOR 3 |
| "EXAMPLE 4" | BEHAVIOR 4 |

### Install — about 30 seconds, once
You need: a claude.ai account. That's it.
1. Download `SKILL-NAME.skill` from the block above.
2. claude.ai → Settings → Capabilities → Skills → upload the file.
3. Done. It stays on across every chat.

> Don't see a Skills section yet? Start a chat, attach the `.skill` file, and tell Claude: "load this skill and use it from now on."

### How to use it
Just ask. Copy-paste starters:
- USAGE PROMPT 1
- USAGE PROMPT 2
- USAGE PROMPT 3

You don't have to phrase it perfectly. The skill's job is to JOB-IN-ONE-CLAUSE.

### When to use it (and when not to)
Use it for GOOD CASES. Skip it when HONEST BAD CASES — BEHAVIOR is a feature, not a substitute for judgment.

### One note before you load it
`SKILL-NAME.skill` is a single plain-text file — open it in any text editor first and read it. It's short, human-readable, and does exactly one thing: change how Claude answers. It doesn't fetch anything from the internet, doesn't run code, and doesn't touch anything outside your chat. That's a good habit for any skill file you ever load, from me or anyone else.

Loaded it and it worked? CTA — AUTHOR HANDLE
